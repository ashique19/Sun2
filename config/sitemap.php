<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Storage path (relative to storage/app)
    |--------------------------------------------------------------------------
    */
    'directory' => 'sitemaps',

    /*
    |--------------------------------------------------------------------------
    | Products per child sitemap file (Google max 50,000)
    |--------------------------------------------------------------------------
    */
    'products_per_file' => 5000,

    /*
    |--------------------------------------------------------------------------
    | Products processed per Livewire/HTTP chunk (progress updates)
    |--------------------------------------------------------------------------
    */
    'chunk_size' => 250,

    /*
    |--------------------------------------------------------------------------
    | Debounce auto-rebuilds after product changes (seconds)
    |--------------------------------------------------------------------------
    */
    'auto_rebuild_debounce_seconds' => 120,

    /*
    |--------------------------------------------------------------------------
    | Secret token for HTTP rebuild (hosting panel cron / curl)
    |--------------------------------------------------------------------------
    |
    | GET /internal/sitemap/rebuild?token=...
    |
    */
    'rebuild_token' => env('SITEMAP_REBUILD_TOKEN'),

];
